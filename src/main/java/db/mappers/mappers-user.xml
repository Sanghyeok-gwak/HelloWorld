<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>

	<!-- admin파트 시작 -->
	<entry key="selectAllUser">
		SELECT
			  USER_NO
			 ,USER_ID
			 ,USER_PWD
			 ,USER_NAME
			 ,EMAIL
			 ,PHONE
			 ,ENROLL_DATE
			 ,MODIFY_DATE
			 ,STATUS
		FROM "USER"
	   WHERE
			 ROLL IN('U','M')
		ORDER BY ROLL, USER_NO DESC
	</entry>
	<entry key="deletUser">
		UPDATE "USER"
   		   SET STATUS = 'R'
 		 WHERE USER_NO = ? 
	</entry>
	<entry key="idCheck">
		SELECT COUNT(*) as cnt
		  FROM "USER"
		 WHERE USER_ID = ?
	</entry>
	<entry key="addUser">
		INSERT 
		  INTO "USER" 
		VALUES (
		        SEQ_USER.NEXTVAL
		      , ?
		      , ?
		      , ?
		      , ?
		      , ?
		      , SYSDATE 
		      , SYSDATE 
		      ,'U'
		      ,'U')
	</entry>
	<entry key="selectUserByUserNo">
		SELECT
			  USER_NO
			 ,USER_ID
			 ,USER_PWD
			 ,USER_NAME
			 ,EMAIL
			 ,PHONE
		FROM "USER"
	   WHERE
			 USER_NO = ?
	</entry>
	<entry key="modifyUser">
		UPDATE "USER"
   	 	   SET USER_PWD = ?,
       		   USER_NAME = ?,
      	       EMAIL = ?,
       		   PHONE = ?,
       		   MODIFY_DATE = SYSDATE
 		 WHERE USER_NO = ?
	</entry>
	<!-- admin파트 종료 -->

	<!-- 사용자 파트 -->
	<entry key="loginUser">
		SELECT
			  USER_NO
			, USER_ID
			, USER_PWD
			, USER_NAME
			, EMAIL
			, PHONE
			, ENROLL_DATE
			, MODIFY_DATE
			, ROLL
			, STATUS
		FROM
		    USER
	  WHERE
	  		USER_ID = ?
	  	AND USER_PWD = ?
	</entry>

</properties>